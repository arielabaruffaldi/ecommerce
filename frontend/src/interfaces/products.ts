export interface Product {
    code: string
    id: number
    name: string
    price: number
    stock: number
    description: string
    color: string
    thumbnail: string
    timestamp: number
    quantity?: number
}

